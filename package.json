{
  "name": "polygon-nft",
  "version": "1.0.0",
  "description": "",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "npm run --workspace=contract",
    "client": "npm run --workspace=client",
    "test": "npm run --workspace=contract test"
  }
}
